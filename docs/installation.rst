============
Installation
============

At the command line::

    $ easy_install db_designer_web

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv db_designer_web
    $ pip install db_designer_web
